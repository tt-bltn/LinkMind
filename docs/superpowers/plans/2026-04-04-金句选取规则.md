# 金句选取规则 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将两个 SKILL.md 文件中的金句选取指令升级为三类型配额制，附带近似时间戳计算规则。

**Architecture:** 纯文本修改，无代码变更。两个 SKILL.md 文件中的 `**金句摘录：**` 块替换为相同的新内容，内容包含类型配额规则、降级机制、过滤条件和时间戳计算公式。

**Tech Stack:** Markdown 文本编辑

---

## 文件变更清单

| 操作 | 文件 | 变更位置 |
|------|------|---------|
| 修改 | `.claude/skills/linkmind-capture/SKILL.md` | 第 244–263 行（`**金句摘录：**` 块） |
| 修改 | `skills/linkmind/SKILL.md` | 第 291–310 行（`**金句摘录：**` 块） |

两个文件的替换内容完全相同。

---

## 新的金句摘录块内容

以下是两个文件中 `**金句摘录：**` 到选取规则结束之间需要替换的完整新内容：

```
**金句摘录：**

(Read the SRT file. Parse total entry count (N_total) and estimate video duration
from the last entry's end timestamp. If end timestamp is unavailable, use
N_total × 3 seconds as the total duration.

Select 3 quotes using the type-quota system below. For each selected quote at
SRT entry index i, calculate:
  approx_seconds = (i / N_total) × total_duration_seconds
  percent = round(i / N_total × 100)
  display as: `~MM:SS`（视频约 P% 处）

**Type quota — select one quote of each type, in priority order:**

1. **观点型** — Expresses a distinctive insight or counter-intuitive conclusion.
   Signal phrases: 「本质是」「真正决定」「往往不是…而是」「关键不在于」

2. **概念定义型** — Clearly defines a core concept.
   Signal phrases: 「等于」「就是」「定义为」「翻译成人话」「说白了」

3. **行动指导型** — Directly actionable guidance.
   Signal phrases: 「应该」「需要」「不要…要」「第一步」「关键是要」

**Fallback rule:** If a type has no suitable candidate in the full transcript,
substitute the best remaining quote of any type. Max 1 fallback per note.
Never use the same type twice across the 3 quotes.

**Universal filters — exclude any entry that:**
- Starts with pure filler with no substantive content (e.g., only "呢", "啊", "那个那个")
- Has an SRT entry index within N_total × 10% of an already-selected entry (avoid clustering)

Do NOT label quote types in the note output.:)

> "（金句原文）"
> —— `~MM:SS`（视频约 X% 处）

> "（金句原文）"
> —— `~MM:SS`（视频约 X% 处）

> "（金句原文）"
> —— `~MM:SS`（视频约 X% 处）
```

---

## Task 1: 更新 `.claude/skills/linkmind-capture/SKILL.md`

**Files:**
- Modify: `.claude/skills/linkmind-capture/SKILL.md`（第 244–263 行）

- [ ] **Step 1: 确认当前内容**

  打开文件，找到 `**金句摘录：**` 块，确认需要替换的范围是从 `**金句摘录：**` 这一行到以下这行（含）：
  ```
  - Do NOT pick 3 consecutive or near-consecutive entries
  ```

- [ ] **Step 2: 替换金句摘录块**

  将以下旧内容：
  ```
  **金句摘录：**

  (Read the SRT file and select the 3 most insightful or quotable sentences from the
  full transcript. Format each as a blockquote with its timestamp on the next line.
  Use the start timestamp of the SRT entry, dropping milliseconds, format `HH:MM:SS`:)

  > "（金句原文）"
  > —— `HH:MM:SS`

  > "（金句原文）"
  > —— `HH:MM:SS`

  > "（金句原文）"
  > —— `HH:MM:SS`

  **Selection criteria for quotes:**
  - Choose sentences that best capture a core insight, key argument, or memorable phrasing
  - Each quote must be a complete, standalone statement — not a sentence fragment
  - Spread timestamps across the video (one from early, one from middle, one from late)
  - Do NOT pick 3 consecutive or near-consecutive entries
  ```

  替换为（见上方"新的金句摘录块内容"）。

- [ ] **Step 3: 验证替换结果**

  检查替换后的文件，确认：
  - `HH:MM:SS` 字样已消失
  - `~MM:SS`（视频约 X% 处）格式已存在
  - `Type quota` 三类型定义已存在
  - `Fallback rule` 降级规则已存在
  - `Universal filters` 两条过滤条件已存在

  运行：
  ```bash
  grep -n "HH:MM:SS" .claude/skills/linkmind-capture/SKILL.md
  ```
  期望输出：无结果（exit 1）

  ```bash
  grep -n "Type quota\|Fallback rule\|Universal filters\|视频约" .claude/skills/linkmind-capture/SKILL.md
  ```
  期望输出：4 行均找到。

- [ ] **Step 4: Commit**

  ```bash
  git add .claude/skills/linkmind-capture/SKILL.md
  git commit -m "feat(skill): upgrade 金句 selection to 3-type quota with timestamp formula"
  ```

---

## Task 2: 更新 `skills/linkmind/SKILL.md`

**Files:**
- Modify: `skills/linkmind/SKILL.md`（第 291–310 行）

- [ ] **Step 1: 确认当前内容**

  找到 `**金句摘录：**` 块，确认需要替换的范围与 Task 1 相同（从 `**金句摘录：**` 到 `Do NOT pick 3 consecutive or near-consecutive entries` 行）。

- [ ] **Step 2: 替换金句摘录块**

  替换内容与 Task 1 Step 2 完全相同。

- [ ] **Step 3: 验证替换结果**

  ```bash
  grep -n "HH:MM:SS" skills/linkmind/SKILL.md
  ```
  期望输出：无结果（exit 1）

  ```bash
  grep -n "Type quota\|Fallback rule\|Universal filters\|视频约" skills/linkmind/SKILL.md
  ```
  期望输出：4 行均找到。

- [ ] **Step 4: 验证两个文件的金句块内容一致**

  ```bash
  diff \
    <(awk '/\*\*金句摘录：\*\*/{f=1} f{print} /Do NOT label quote types/{f=0}' \
        .claude/skills/linkmind-capture/SKILL.md) \
    <(awk '/\*\*金句摘录：\*\*/{f=1} f{print} /Do NOT label quote types/{f=0}' \
        skills/linkmind/SKILL.md)
  ```
  期望输出：无 diff（两文件该块完全相同）。

- [ ] **Step 5: Commit**

  ```bash
  git add skills/linkmind/SKILL.md
  git commit -m "feat(skill): sync 金句 selection rules to main SKILL.md"
  ```
